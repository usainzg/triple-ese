//División por 0
program prueba3 is

var a,b : integer ;

begin
  a := b + 6 * 6 - 4;
  while (a+3 < b-3) do
    a := a * 2;
    exit if b < 0;
    b := b + 1;
    b:= a / (b-9);
  endwhile ;
  a:=b;

endprogram ;
